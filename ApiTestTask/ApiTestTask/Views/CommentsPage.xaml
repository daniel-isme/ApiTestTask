<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ApiTestTask.Views.CommentsPage">

    <ContentPage.Content>
        <CollectionView ItemsSource="{Binding Comments}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout HeightRequest="60" VerticalOptions="Center">
                        <BoxView HorizontalOptions="FillAndExpand" HeightRequest="2" Color="#dbdbdb"/>
                        <Grid Margin="0,9,0,0">
                            <Label Grid.Column="0" Text="{Binding Name}" FontSize="Medium" TextColor="#4f596e" HorizontalTextAlignment="End"/>
                            <Label Grid.Column="1" Text="{Binding Comment}"
                                           FontAttributes="Bold" FontSize="Medium" TextColor="#4f596e" HorizontalTextAlignment="End"/>
                        </Grid>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ContentPage.Content>
</ContentPage>